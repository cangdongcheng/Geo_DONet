#!/bin/bash
#PBS -N Geo_DeepONet_PCA
#PBS -l select=1:ncpus=8:ngpus=1:mem=64gb
#PBS -l walltime=01:00:00
#PBS -j oe
#
# Prepare the compact feature file on a CPU node first (README.md), then submit
# this script from Geo_DeepONet_PCA/.  Do not add `set -e`: the environment
# loader's initial conda deactivate may return status 1.

source ~/load_dimon_env.sh
cd /home/svu/e1032484/Geo_DONet/Geo_DeepONet_PCA

echo "=== START $(date) ==="
echo "node: $(hostname)"

python -u main.py \
    --device cuda \
    --epochs 5000 \
    --batch-size 8 \
    --nodes-per-step 2048 \
    --val-nodes 4096 \
    --n-components 5 \
    --feature-loss-weight 0 \
    --lr 5e-4 \
    --patience 1000

echo "=== DONE $(date) ==="
